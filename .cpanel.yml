---
deployment:
  tasks:
    - export DEPLOYPATH=/home/jurnalt1/public_html/
    - /bin/cp index.html $DEPLOYPATH
    - /bin/cp -R assets $DEPLOYPATH
    - /bin/cp -R images $DEPLOYPATH
    - /bin/cp -R scss $DEPLOYPATH
